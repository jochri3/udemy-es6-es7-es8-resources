const calculSomme = function (nombre1, nombre2) {
  return nombre1 + nombre2;
};

const eleverAuCarre = (nombre) => Math.pow(nombre, 2);

const direBonjour = () => console.log('Bonjour à tous');

const calculSommeArrow = (nombre1, nombre2) => nombre1 + nombre2;

const somme = calculSomme(4, 5);
